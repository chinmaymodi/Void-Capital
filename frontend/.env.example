# Void Capital frontend -- environment variable template
# Copy to .env and fill in. VITE_ vars are embedded at build time.

# Admin API key from src/VoidCapital.Api/auth.keys.json (gitignored).
# The dashboard views all users, so it authenticates as admin.
VITE_API_KEY=