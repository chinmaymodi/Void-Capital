// Placeholder for routes not yet built (D4.1 ships Dashboard + Holdings only).

export default function Placeholder({ title }: { title: string }) {
  return (
    <div className="placeholder-page">
      <h1>{title}</h1>
      <p>This page is planned for a later ticket.</p>
    </div>
  );
}
